# Generated by Cargo
# will have compiled files and executables
debug
target

# Local working notes
NOTES.md

# The ground station key. Never commit one: it is the shared secret the whole
# link rests on, and a copy in git history is a copy that cannot be revoked.
*.key

# APKs and their signing sidecars, from `x run` / `cargo apk`
*.apk
*.idsig

# Recorded streams and captures kept beside the app while testing
*.h264
*.h265
*.ts

# These are backup files generated by rustfmt
**/*.rs.bk

.vscode/

# Written by the Settings page; per-machine, not source.
drone-app.toml
